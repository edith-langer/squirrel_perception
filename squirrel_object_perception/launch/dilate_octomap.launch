<?xml version="1.0"?>
<launch>

<!-- scene database (MongoDB) -->
   <node name="rosplan_scene_database" pkg="mongodb_store" type="mongodb_server.py" respawn="false" output="log">
	    <param name="database_path" value="$(find rosplan_knowledge_base)/common/mongoDB" />
   </node>
   <node name="rosplan_scene_message_store" pkg="mongodb_store" type="message_store_node.py" respawn="false" output="log">
   </node>

  <node name="squirrel_dilate_octomap"
        pkg="squirrel_object_perception"
        type="squirrel_dilate_octomap"
        required="true" output="screen">
	<param name="octomap_path" value="$(find squirrel_object_perception)/data/octomaps/GH30_last_edited.bt" />
	<param name="dilated_octomap_path" value="$(find squirrel_object_perception)/data/octomaps/GH30_last_dilated.bt" />
  </node>
</launch>
